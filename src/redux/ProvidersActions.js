import {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
} from "./ProvidersActionCreators";

const providerActions = {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
};

export default providerActions;
