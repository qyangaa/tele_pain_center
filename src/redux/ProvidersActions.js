import {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
  selectProvider,
} from "./ProvidersActionCreators";

const providerActions = {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
  selectProvider,
};

export default providerActions;
