import {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
} from "./ActionCreators";

const allActions = {
  fetchProviders,
  addProviders,
  providersLoading,
  providersFailed,
};

export default allActions;
